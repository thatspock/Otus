import SwiftUI

struct MarketsView: View {
    var body: some View {
        VStack {
            Text("hi, there!")
        }
    }
}

struct MarketsView_Previews: PreviewProvider {
    static var previews: some View {
        MarketsView()
    }
}
